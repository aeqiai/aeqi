name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-ui:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/ui/package-lock.json
      - run: npm --prefix apps/ui ci
      - run: npm --prefix apps/ui run build
      - uses: actions/upload-artifact@v4
        with:
          name: ui-dist
          path: apps/ui/dist/

  build:
    needs: build-ui
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            name: aeqi-linux-amd64
          # ARM Linux temporarily disabled — cross-compile openssl/libc issues
          # - target: aarch64-unknown-linux-gnu
          #   os: ubuntu-latest
          #   name: aeqi-linux-arm64
          #   cross: true
          - target: aarch64-apple-darwin
            os: macos-latest
            name: aeqi-darwin-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: release-${{ matrix.target }}
      - uses: actions/download-artifact@v4
        with:
          name: ui-dist
          path: apps/ui/dist/

      - name: Install ARM cross-compiler
        if: matrix.cross
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc" >> $GITHUB_ENV

      - name: Build
        run: cargo build --release -p aeqi --target ${{ matrix.target }}

      - name: Package
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/aeqi dist/${{ matrix.name }}
          chmod +x dist/${{ matrix.name }}

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: dist/${{ matrix.name }}

  release:
    needs: build
    runs-on: ubuntu-latest
    if: always() && needs.build.result != 'cancelled'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Checksums
        run: |
          cd artifacts
          find . -name "aeqi-*" -type f | while read f; do sha256sum "$f"; done > SHA256SUMS.txt
          cat SHA256SUMS.txt

      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/aeqi-linux-amd64/aeqi-linux-amd64
            artifacts/aeqi-darwin-arm64/aeqi-darwin-arm64
            artifacts/SHA256SUMS.txt
