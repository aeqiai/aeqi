#!/usr/bin/env bash
# Pre-push gate: thin wrapper around scripts/ci-local.sh so git invokes the
# canonical local CI contract before push.

set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
exec "$ROOT/scripts/ci-local.sh" prepush
